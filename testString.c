#include <stdio.h>
#include <conio.h>
int main(void){
    char name[12];
    short level;
    short i;
    while (1)
    {
        //i=scanf("%[^ ]%d",name,&level);
        i=scanf("%s %d",name,&level);
        puts(name);
    }
    
}
